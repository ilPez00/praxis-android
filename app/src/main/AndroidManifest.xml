<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for future features -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Praxis">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Praxis">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- WebView Activity for Praxis Web App -->
        <activity
            android:name=".WebAppActivity"
            android:exported="false"
            android:theme="@style/Theme.Praxis"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Goal Progress Widget -->
        <receiver
            android:name=".widget.ProgressWidgetProvider"
            android:exported="true"
            android:label="Goal Tracker">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.praxis.app.ACTION_INCREMENT_PROGRESS" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/progress_widget_info" />
        </receiver>
        <!-- Statistics Widget -->
        <receiver
            android:name=".widget.StatsWidgetProvider"
            android:exported="true"
            android:label="Praxis Stats">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/stats_widget_info" />
        </receiver>

        <!-- Axiom Last Message Widget -->
        <receiver
            android:name=".widget.AxiomWidgetProvider"
            android:exported="true"
            android:label="Axiom Chat">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/axiom_widget_info" />
        </receiver>

        <!-- Daily Message Widget -->
        <receiver
            android:name=".widget.DailyMessageWidgetProvider"
            android:exported="true"
            android:label="Daily Axiom Quote">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/daily_message_widget_info" />
        </receiver>

        <!-- Tracker Summary Widget -->
        <receiver
            android:name=".widget.TrackerWidgetProvider"
            android:exported="true"
            android:label="Praxis Trackers">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/tracker_widget_info" />
        </receiver>
    </application>

</manifest>
