<?xml version="1.0" encoding="utf-8"?>
<!--
  updatePeriodMillis is 0 on every Praxis widget on purpose. The framework's own
  polling has a 30-minute floor and wakes the device to fire it; WidgetRefreshWorker
  already refreshes under WorkManager's constraints, so enabling both would mean
  paying for two schedulers to do one job.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_streak"
    android:previewLayout="@layout/widget_streak"
    android:description="@string/widget_streak_description"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />
